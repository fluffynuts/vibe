# Settings for the dotnet feature, merged into the settings.yaml of any
# profile composed with it. Without nugetDir set, vibe mounts nothing and
# never passes SBX_HOST_NUGET, so the feature's own link-nuget has nothing
# to link and logs "SBX_HOST_NUGET unset — nothing to link".
nugetDir: ~/.nuget
