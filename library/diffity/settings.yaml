# Settings for the diffity feature, merged into the settings.yaml of any
# profile composed with it: the viewer's port is published to the host, and
# the sandbox gets its URL in $VIBE_DIFFITY_URL.
publish:
  - name: diffity
    ports:
      - 5391
    urlEnv: VIBE_DIFFITY_URL
