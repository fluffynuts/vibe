#!/bin/sh
# vibe: description: Install mysql-server and the file utility
# vibe: user: 0
echo "--- installing mysql, file util $(date -Is)" >>/tmp/sbx-setup.log
sudo DEBIAN_FRONTEND=noninteractive apt-get -o DPkg::Lock::Timeout=300 install -y mysql-server file </dev/null >>/tmp/sbx-setup.log 2>&1 \
  || echo "WARN: install failed" >>/tmp/sbx-setup.log
true
